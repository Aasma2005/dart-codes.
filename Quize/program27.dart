void main(){
  int totalpoint=900;
  int teamscore=1200;
  int fairplaypoint=-300;

  if(totalpoint >= (teamscore-fairplaypoint)){
    print("team qualified");
  }
  else{
     print("team not qualified");
  }
   print("total point are more than fairplaypoints");
}